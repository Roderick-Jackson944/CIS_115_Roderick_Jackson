import math
#defines var
x = float(input ('input x variable: '))
y = float(input ('input y variable: '))
pie = 180 / float(math.pi)
tan = math.atan2(y, x)
c = float(tan) * float(pie)
#print Result
print('{0} is the Theta angle of your triangle'.format(c))
